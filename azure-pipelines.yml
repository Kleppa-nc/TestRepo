trigger:
  - master
  - main
pool:
  vmImage: 'ubuntu-latest'
stages:
  - stage: cleaninstallpack
    jobs:
      - job: build
  - template: maven-clean-template.yaml
  - template: maven-install-template.yaml
  - template: maven-package-template.yaml

